# 智能邮件监控与微信通知 Agent 实施方案 (MailAssist)

本项目旨在构建一个私有化部署的智能邮件助手，监听多个邮箱（Gmail、QQ 邮箱、其他企业/高校邮箱），通过大语言模型（LLM）识别**重要待办事项**与**高相关度学术论文（Scholar / arXiv）**，并直接推送到个人微信。

---

## 一、 总体架构设计

```
                         ┌────────────────────────┐
                         │      多邮箱接入层      │
                         │ - Gmail (API / IMAP)   │
                         │ - QQ 邮箱 (IMAP SSL)   │
                         │ - 其他邮箱 (IMAP SSL)  │
                         └───────────┬────────────┘
                                     │ 定时轮询 / 新邮件抓取
                                     ▼
                         ┌────────────────────────┐
                         │      邮件解析与过滤    │
                         │ - Message-ID 去重      │
                         │ - 垃圾/营销邮件规则快筛│
                         │ - 正文/附件/链接提取   │
                         └─────┬────────────┬─────┘
                               │            │
             [普通/业务邮件]   │            │   [学术推送邮件]
                               ▼            ▼
         ┌───────────────────────┐   ┌────────────────────────┐
         │ 1. 事务型分析模块     │   │ 2. 学术论文解析与阅读  │
         │ - 提取待办/紧急程度   │   │ - 提取 arXiv/DOI/链接  │
         │ - 提取 Deadline/动作  │   │ - 抓取 Abstract / PDF  │
         │ - 决策是否需要通知    │   │ - 与个人研究画像比对   │
         └───────────┬───────────┘   └───────────┬────────────┘
                     │                           │
                     └─────────────┬─────────────┘
                                   │ 结构化通知数据
                                   ▼
                         ┌────────────────────────┐
                         │   微信消息推送模块     │
                         │ - 企业微信应用 API     │
                         │ - Markdown / 卡片消息  │
                         │ - 直达个人微信聊天列表 │
                         └────────────────────────┘
```

---

## 二、 核心功能模块详细设计

### 1. 多邮箱监听与状态管理
* **支持协议与邮箱**：
  * **QQ 邮箱 / 163 邮箱 / 高校邮箱**：开启 IMAP 服务，使用专属授权码登录，基于 `imaplib` / `aioimaplib` 监听 `UNSEEN` 邮件。
  * **Gmail**：支持两种模式：
    * 模式 A（推荐）：开启 Google 账号两步验证，生成“应用专用密码”，直接通过 IMAP 连接。
    * 模式 B：通过 Google Cloud 申请 OAuth2 Credentials，调用 Gmail REST API。
* **状态存储与幂等性**：
  * 使用本地轻量数据库 `SQLite` 存储已读邮件的 `message_id`、处理状态、判定结果与时间戳，保证断点恢复与不重复通知。
* **规则预过滤（节省 Token）**：
  * 自动跳过已被系统标记为垃圾、广告订阅（如无实质内容的每周促销、社交通知）。

---

### 2. 事务分析：重要事项识别
* **提取内容**：发件人、主题、正文文本（自动将 HTML 转为清晰纯文本）、附件列表。
* **LLM 评估逻辑**：
  * **维度判定**：
    1. **重要度评分（1-5）**：涉及工作推进、项目审批、财务账单、账号安全、会议日程等。
    2. **紧急度/时效性**：是否包含明确的截止时间（Deadline）或需要即刻回复/操作。
    3. **处理动作建议**：提取出“用户需要做什么”（例如：“需在今日 18:00 前回复确认”、“需登录系统审批”）。
  * **通知阈值**：设定评分阈值（例如重要度 $\ge 3$ 或包含明确待办），触发微信通知。

---

### 3. 学术推送：Scholar & arXiv 深度阅读与筛选
* **识别来源**：
  * Google Scholar 关键词/引用提醒推送。
  * arXiv Daily/Weekly 订阅或相关领域通知。
* **论文定位与内容抓取**：
  * **arXiv**：自动正则提取 arXiv ID（如 `2403.xxxxx`），直接请求 arXiv 官方 API 获取官方标准元数据、摘要；若需要进一步精读，则抓取对应 PDF 文本。
  * **Google Scholar / 其他出版物**：提取邮件内的论文标题与 DOI 链接，通过 Semantic Scholar API / Crossref 抓取 Abstract；对开源论文进一步抓取 PDF 导读。
  * **PDF 阅读能力**：集成 `PyMuPDF` (fitz) 或 `pypdf`，针对长文自动截取 Introduction 与 Conclusion 部分送入大模型，避免上下文超长。
* **相关度评估**：
  * 维护一份本地用户配置文件 `user_profile.yaml`，声明用户的研究方向、感兴趣的技术、核心关键词、当前课题。
  * **LLM 打分与精读报告**：
    * 计算相关度（0 - 100 分），设定阈值（如 $\ge 70$ 分推送）。
    * 自动生成包含：**【论文标题】**、**【核心贡献/方法】**、**【与我的相关点】**、**【PDF/论文链接】** 的中文结构化速读卡片。

---

### 4. 微信通知通道（企业微信应用模式）
* **接收体验**：
  * 手机端**无需下载企业微信 App**。
  * 在个人微信中进入“设置 -> 通用 -> 辅助功能”开启“企业微信”，接收的消息会直接出现在**个人微信的常规聊天列表**中，并有普通聊天弹窗提醒。
* **消息展现形式**：
  * 采用企业微信的 `markdown` 或 `textcard`（文本卡片）消息类型，排版清晰，支持一键点击外链直达论文或邮件附件。

---

## 三、 个人免认证企业微信配置全流程指南

> **请按以下步骤操作，完成后只需提供第 4 步中的 3 个参数即可：**

### 步骤 1：创建免费企业/团队（1 分钟）
1. 电脑浏览器打开 [企业微信官网](https://work.weixin.qq.com/)。
2. 点击右上角 **“企业登录 / 免费注册”**。
3. 选择 **“快速注册”**：
   * 企业类型选：**团队 / 个体**（**不需要**营业执照，个人可随意填）。
   * 企业名称：填一个你喜欢的名字（如 `我的个人助手`）。
   * 按照提示用个人微信扫码完成注册。

### 步骤 2：在企业微信中创建应用（1 分钟）
1. 注册成功后进入企业微信后台管理平台。
2. 导航栏进入 **“应用管理”** -> 滚动到下方找到 **“自建”** -> 点击 **“创建应用”**。
3. 填写应用信息：
   * 应用 Logo：可随意上传一个图标。
   * 应用名称：如 `邮件通知`。
   * **可见范围**：非常关键！必须点击添加，选择你自己的账号（即你自己这个成员）。
4. 点击“创建应用”，创建成功后进入该应用详情页。

### 步骤 3：开启“在个人微信中接收消息”（核心）
1. 在管理后台顶部导航栏点击 **“我的企业”**。
2. 点击左侧菜单中的 **“微信插件”**。
3. 页面会展示一个**二维码**，使用你的**个人微信扫码关注**。
4. 扫码关注后，确保勾选了“在微信中接收消息”。
5. （此时你的个人微信通讯录里就会出现该企业插件，后续所有推送都会当作微信消息通知你）。

### 步骤 4：提取系统需要的 3 个核心凭据
在配置和启动 Agent 时，我们需要在配置文件中填入：
1. **`corp_id` (企业 ID)**：
   * 位于后台 **“我的企业”** -> 页面最下方的 **“企业 ID”** (以 `ww...` 开头)。
2. **`agent_id` (应用 AgentId)**：
   * 位于 **“应用管理”** -> 点击你刚创建的应用 -> **AgentId**（纯数字）。
3. **`corp_secret` (应用 Secret)**：
   * 位于刚创建的应用页面 -> **Secret** -> 点击“查看”，企业微信会发送一条确认消息到你手机微信上，点击确认后页面即可复制该 Secret。

---

## 四、 推荐技术栈与工程结构

* **语言环境**：Python 3.10+
* **核心依赖**：
  * 邮箱操作：`imaplib` / `email` / `beautifulsoup4`
  * 论文获取与解析：`httpx` / `PyMuPDF` (fitz)
  * LLM 框架：支持 OpenAI 兼容格式（如 Claude / GPT-4o / DeepSeek / Kimi / 本地 Ollama 等）
  * 微信推送：原生调用企微 `cgi-bin/message/send` API
  * 数据持久化：`SQLite3`
  * 定时调度：`APScheduler`

* **项目目录规划**：
  ```
  mail_assist/
  ├── config.yaml          # 邮箱凭证、微信凭据、大模型配置
  ├── user_profile.yaml    # 个人研究方向、关注关键词配置
  ├── mail_assist/
  │   ├── __init__.py
  │   ├── mail_client.py   # 多邮箱 IMAP/POP 连接与拉取
  │   ├── parser.py        # 邮件正文清洗、链接与附件提取
  │   ├── scholar.py       # arXiv / Scholar 论文抓取与 PDF 处理
  │   ├── agent.py         # LLM 意图判断、重要度评分、论文速读评估
  │   ├── notifier.py      # 企业微信通道推送
  │   ├── storage.py       # SQLite 去重与日志记录
  │   └── scheduler.py     # 任务调度循环
  ├── run.py               # 服务启动主入口
  └── README.md
  ```

---

## 五、 后续实施步骤

当你准备好邮箱授权码及企业微信凭据后，我们将按以下步骤分步实施：
1. **第一阶段**：初始化工程，实现企业微信推送模块与多邮箱（QQ/Gmail）联通性测试。
2. **第二阶段**：编写邮件解析与 SQLite 去重模块，建立大模型事务分析 Agent。
3. **第三阶段**：开发学术专线（arXiv/Scholar 抓取、PDF 解析、相关性评估与自动总结）。
4. **第四阶段**：端到端整合测试与后台守护运行部署。
